# The Backend Engine For TradeLikeBot
A Bot, initially developed by a freelancer and then iterated over by us

### To restart the celery worker & app with changes in Docker:
```bash
docker-compose down
docker-compose up --build
```
